'use strict'

const authEvents = require('./auth/events')
const authUI = require('./auth/ui')
const gameEngine = require('./game/engine')
const gameEvents = require('./game/events')
const gameUI = require('./game/ui')

$(() => {
  $('#sign-up').on('submit', authEvents.onSignUp)
  $('#sign-in').on('submit', authEvents.onSignIn)
  $('#delta-form').on('submit', authEvents.onChangePassword)
  $('#sign-out').on('submit', authEvents.onSignOut)
  $('#create-game').on('submit', gameEvents.onCreateGame)
  $('.col-4.box').on('click', gameEngine.togglePlayer)
  $('.col-4.box').on('click', gameEngine.moveMessage)
  // $('#0').on('click', gameUI.onCoordinateZero)
  // $('#1').on('click', gameUI.onCoordinateOne)
  // $('#2').on('click', gameUI.onCoordinateTwo)
  // $('#3').on('click', gameUI.onCoordinateThree)
  // $('#4').on('click', gameUI.onCoordinateFour)
  // $('#5').on('click', gameUI.onCoordinateFive)
  // $('#6').on('click', gameUI.onCoordinateSix)
  // $('#7').on('click', gameUI.onCoordinateSeven)
  // $('#8').on('click', gameUI.onCoordinateEight)
  $('#game-stats').on('click', gameEvents.onGameStats)
  $('#play-game-yes').on('click', gameUI.onPlayTrue)
  $('#play-game-no').on('click', gameUI.onPlayFalse)
  $('#open-register-form').on('click', gameUI.onOpenRegister)
  $('#open-authenticate-form').on('click', gameUI.onOpenAuthenticate)
  $('#delta-submit-check').on('click', authUI.onDeltaSubmitCheck)
  $('#delta-submit-facade').on('click', authUI.onDeltaSubmitFacade)
  $('#delta-submit-y').on('click', authUI.onPasswordCheckYes)
  $('#delta-submit-n').on('click', authUI.onPasswordCheckNo)
})
